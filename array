#include <stdio.h>
#include <stdlib.h>

int main()
{
int n, temp;
scanf("%d", &n);//input for array length
int arr[n];
for(int i=0;i<n;i++)
{
    scanf("%d", &arr[i]);//input for array values
}
for(int i=n-1;i>=0;i--)
{
    printf("%d ",arr[i]);//reversed output
}
return 0;
}
